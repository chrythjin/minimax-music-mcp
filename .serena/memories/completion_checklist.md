# Completion Checklist

After implementation changes, run:

- `npm run build`
- `npm run test`

For Final Implementation Plan work, append a timestamped note to `.sisyphus/notepads/minimax-music-mcp-final-implementation-plan/learnings.md` or `issues.md` and never modify `.sisyphus/plans/*.md`. If code/config changed successfully, also create a session record under `docs/sessions/YYYYMMDD_HHMMSS_<task-name>.md` per global workflow.